export default function Footer() {
    return (
        <footer className="wrap py-8">
            <p className="font-mono text-xs text-faint">
                © {new Date().getFullYear()} Nischit Kumar
            </p>
        </footer>
    );
}
